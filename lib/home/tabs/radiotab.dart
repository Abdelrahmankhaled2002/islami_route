import 'package:flutter/material.dart';
class RadoiTab extends StatelessWidget {
  const RadoiTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orangeAccent,
    );
  }
}
